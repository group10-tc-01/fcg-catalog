apiVersion: v1
kind: Service
metadata:
  name: fcg-catalog-service
  namespace: fcg-catalog
  labels:
    app.kubernetes.io/name: fcg-catalog
    app.kubernetes.io/component: api
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: fcg-catalog
  ports:
    - name: http
      port: 80
      targetPort: 8080
